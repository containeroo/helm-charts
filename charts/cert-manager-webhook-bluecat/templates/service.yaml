apiVersion: v1
kind: Service
metadata:
  name: {{ include "bluecat-webhook.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "bluecat-webhook.name" . }}
    chart: {{ include "bluecat-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "bluecat-webhook.name" . }}
    release: {{ .Release.Name }}
